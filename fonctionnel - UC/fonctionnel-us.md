## 🔹 Fonctions pour l’espace Administration – Gestion des Tags

- `getAllTags()` — Affiche tous les tags  
- `storeTag(array $data)` — Ajoute un nouveau tag  
- `editTag(int $id)` — Récupère les données d’un tag pour modification  
- `updateTag(int $id, array $data)` — Enregistre les modifications d’un tag  
- `destroyTag(int $id)` — Supprime définitivement un tag  
